import{describe,expect,it}from'vitest';import{TaskClient,mapBounded}from'../src/tasks/index.js'
describe('tasks',()=>{it('requires idempotency and validates durable task snapshots',async()=>{const caller={async callTool(){return{task:{taskId:'t1',status:'queued'}}}};const c=new TaskClient(caller);await expect(c.submit({type:'x',input:{},idempotencyKey:'k'} as any)).resolves.toMatchObject({task:{taskId:'t1'}});await expect(c.submit({type:'x',input:{}} as any)).rejects.toThrow('idempotencyKey')});it('maps with bounded ordered concurrency',async()=>{let live=0,max=0;const out=await mapBounded([1,2,3,4],async x=>{live++;max=Math.max(max,live);await new Promise(r=>setTimeout(r,5));live--;return x*2},2);expect(out).toEqual([2,4,6,8]);expect(max).toBe(2)})})
